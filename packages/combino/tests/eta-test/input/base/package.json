{
	"name": "<%= it.name.toLowerCase() %>",
	"version": "1.0.0",
	"description": "A <%= it.framework %> project",
	"main": "index.js",
	"scripts": {
		"start": "echo 'Starting <%= it.name %>'",
		"build": "echo 'Building <%= it.name %>'"
	},
	"dependencies": {
		"<%= it.framework.toLowerCase() %>": "^18.0.0"
	},
	"devDependencies": {
		"typescript": "^5.0.0"
	}
}
