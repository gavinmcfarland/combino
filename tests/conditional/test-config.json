{
	"data": {
		"framework": "react",
		"language": "ts",
		"type": "web"
	},
	"inputDirs": ["input/base"],
	"plugins": ["ejs"],
	"description": "Conditional test that only uses the base input directory"
}
